%h1 Requested songs
%p.subtitle Request a song for us to play sometime during the reception.
%p.subtitle
  = link_to 'Request a song', new_song_path, class: "btn btn-primary"
.container-fluid.info-port.song
  %table.table.table-striped
    %thead.thead
      %tr
        %th.col-xs-6= sortable "title"
        %th.col-xs-6= sortable "artist"
    %tbody
      - @songs.each do |song|
        %tr
          %td= song.title
          %td= song.artist

